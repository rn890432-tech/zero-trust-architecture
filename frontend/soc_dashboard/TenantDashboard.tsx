import React from 'react';

const TenantDashboard = () => {
  // Load tenant-specific alerts, incidents, assets, campaigns
  // ...existing code...
  return (
    <div>
      <h2>Tenant SOC Dashboard</h2>
      {/* Alerts, incidents, assets, campaigns UI */}
    </div>
  );
};

export default TenantDashboard;
